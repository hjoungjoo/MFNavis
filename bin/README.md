# Test branch binaries

Cedar Detect binaries are removed. Star detection uses local SEP plus preprocessing.
The historical license notice is retained for provenance; it does not license the solver core.
