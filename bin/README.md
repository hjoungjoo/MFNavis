# Detector binaries

Cedar Detect binaries are removed. Star detection uses MF native detection plus
preprocessing, with SEP only as an extraction fallback.
The historical license notice is retained for provenance; it does not license the solver core.
