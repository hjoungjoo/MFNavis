name: Install pinned MFDS release
description: Download and verify binary assets; no MFDS source checkout or compiler
runs:
  using: composite
  steps:
    - name: Install pinned MFDS binary package
      shell: bash
      working-directory: ${{ github.workspace }}
      run: bash scripts/setup_mfds.sh
