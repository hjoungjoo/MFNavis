# Install as mfnavis.service.d/20-commercial.conf in the sales image.
# Package verification, not environment variables, enforces no native linking.
[Service]
Environment=MF_DETECT_TRANSPORT=process
Environment=MF_PREPROCESS_REDUCTION=numpy
Environment=MF_PREPROCESS_ACCELERATOR=cpu
Environment=PYTHONDONTWRITEBYTECODE=1
