# 밝은 포화 별 보존과 프레임 전달 수정 — 2026-09-16

## 설계 및 검증 계획

맑은 하늘의 RAW 11장에서 기존 포화 제외는 0/11, 포화 제외를 해제한 오프라인 진단은 11/11 성공했다. 운영에서는 포화 제외 자체를 끄지 않고 다음을 적용한다.

- MFDS: 원본 센서 좌표에서 연결된 포화 영역을 판별한다. 96픽셀 이하·가로세로 16픽셀 이하의 작은 중심이고 주변 PSF 광량이 있을 때 보존한다. 넓은 영역·얇고 긴 영역·주변 광량 없는 밝은 점은 계속 마스킹한다.
- Python: MFDS가 이미 적용한 포화 마스크를 중복 적용하지 않는다. SEP 보조 검출은 동일한 작은 포화 중심·주변 PSF 판정을 적용한다.
- 프레임: 공유 상태에서 512 이미지와 해당 RAW를 한 묶음으로 게시한다. 검출기는 확보한 RAW를 그대로 사용하며 공유 슬롯을 다시 읽지 않는다. 불완전한 짝은 미솔빙으로 처리하지 않고 다음 입력을 기다린다.
- 병렬 전처리, RAW 실패·정렬·보정 중 동기 대기, NixOS, 90초각 GoTo 기준은 유지한다.
- 합성 포화 별·조명·불량화소·binning, 동일 RAW 재생, 과거 달/도심/구름 영상, 프레임 경합 회귀와 전체 검사를 수행한 뒤 운영에 적용한다.

## 결과

### 오프라인 검증

- MFDS `34a4806` (검출기 변경 `6ef8ab4`): native 7/7, 관련 Python 102개 통과. ASan/UBSan 및 호스트 누수 검사 7/7 통과.
- 동일 맑은 하늘 RAW 11장: 기존 0/11 → 수정 11/11. 검출+패턴 솔빙 중앙값 2,045.7 → 60.6ms, 수정 RAW RMSE 중앙값 20.99초각. 노출·공유 상태·게시 시간은 제외한 재생 측정이다.
- 달/도심 RAW 6장·전처리 6장은 기존·수정 모두 미솔빙, 구름 전처리 6장은 모두 5/6 성공. 전처리 12장의 검출 좌표는 완전히 동일했다.
- 단순 포화 해제와 달리 작은 포화 중심에도 주변 광량 조건이 적용된다. 작은 조명이 별과 동일한 PSF를 만들 수 있으므로 기존 형태·밀집도·패턴 품질 검증은 함께 유지한다.
- 전체 unit/smoke **2,452개 통과, 2개 skip**. Ruff 검사·포맷 검사·MyPy(205개 파일) 통과. MFDS GitHub CI `35001735341` 성공. SEP 합성 별의 면적이 기존 40픽셀 제한을 초과한 테스트는 제한 이내에서도 포화되는 입력으로 수정했다. 크기 제한을 완화하지 않았다.

### 실측에서 추가 확인한 전환 반복

첫 배포 `bd2b2420`의 실측에서 RAW 3회 성공 후 비동기로 전환하면 `raw_fallback_confirmation` 대기가 실패 backoff를 설정했다. 다음 RAW를 건너뛰어 동기 복구로 돌아갔고 이 동작이 반복됐다. 검출 후보는 48개였으므로 프레임 누락과 다른 문제다.

패턴 솔빙 결과는 해당 시도 직후 한 번만 기록하도록 수정한다. 좌표 게시의 연속성 확인 대기는 검출 실패에 포함하지 않는다. 연속성 확인과 반복된 게시 거절 시 동기 복귀 정책은 유지한다. 첫 실측 기록은 최종 결과와 분리해 보관한다.

### 운영 검증

최종 코드 `630f4989`, MFDS `34a4806`을 main·release에 푸시하고 운영 `/home/pifinder/PiFinder`에 적용했다. 서비스 재시작 후 안정화된 상태에서 telemetry 180초와 RAW 저장 60초를 기록했다.

| 항목 | 기존 telemetry 180초 | 수정 telemetry 180초 | 수정 RAW 저장 60초 |
|---|---:|---:|---:|
| 솔빙 성공 | 139/143 (97.2%) | 242/242 (100%) | 81/81 (100%) |
| 입력 준비 → 게시 중앙값 | 1,051.95ms | 123.71ms | 127.77ms |
| 입력 준비 → 게시 p95 | 3,072.62ms | 176.97ms | 202.10ms |
| 패턴 RMSE 중앙값 | 15.89초각 | 21.85초각 | 23.27초각 |
| 패턴 RMSE p95 | 25.32초각 | 24.60초각 | 27.09초각 |
| 게시 빈도 | 0.766Hz | 1.343Hz | 1.343Hz |
| RAW 유효 솔빙 | 0 | 242 | 81 |
| 병렬 전처리 | 0 | 242 | 81 |

최종 323회 모두 솔빙·게시했고, 모두 보정 준비 상태였다. RAW 후보 0개, 미래 전처리 프레임 사용, 캡처 무결성 오류, 기록 누락, 관측 중 연속성 거절 및 서비스 ERROR/Traceback은 없었다. RAW 12장을 추가 보관했고 두 기록 세션은 정상 종료했다. 세션 ID는 `c9db4fe6bf514a74adcc8267fb32ab57`, `3260897274644481a1dc8f87df7ca905`이다.

기존 실측과 최종 실측은 시간대가 다른 같은 고정 장비의 기록이며, 동일 픽셀의 통제 비교는 위 RAW 11장 재생이다. RMSE는 별 패턴 적합 잔차이며 최종 지향 오차의 독립 실측값이 아니다. 현재 RAW의 패턴 RMSE 중앙값은 기존 전처리보다 높지만 전처리 기반 좌표 보정이 계속 적용된다. 이번 수정으로 정확도까지 향상됐다고 해석하지 않는다.

- 서비스 PID `104347`, active/enabled, 자동 재시작 0. MFDS 서버 2개가 운영 서브모듈의 바이너리를 사용한다.
- 최종 확인 시 RAW 연속 성공 385회, async·bias_ready 상태. 마운트 이동은 수행하지 않았으므로 물리 GoTo 동작은 이번 검증에 포함하지 않았다. 완료 기준 1.5분각(90초각) 유지.
- 전체 unit/smoke 2,452개, 전환 보완 후 관련 69개, Ruff/포맷/MyPy 통과. 최종 코드의 GitHub CI main `35002558173`·release `35002558371`은 보고서 작성 시 실행 중이었다.

기존 `m2.6.4` 태그는 보존한다. 원본 영상·관측 좌표·설정은 로컬 `PiFinder_test_data/work/compact_saturation_fix_20260916/` 및 캡처 세션에 보관한다.
